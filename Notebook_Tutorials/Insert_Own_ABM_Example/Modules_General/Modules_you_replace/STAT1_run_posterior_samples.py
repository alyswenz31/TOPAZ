def run_posterior_samples(
    model_name,
    posterior,
    num_samples,
    output_path,
    simulate_abm,
    rng,
):
    """Draw posterior parameters and run an ABM; replace for your model."""
    raise NotImplementedError(
        "Connect run_posterior_samples to your posterior representation and ABM simulator."
    )
